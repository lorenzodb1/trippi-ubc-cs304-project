{% extends 'templates/app.twig' %}

{% block content %}

    <div class="row">
        <div class="col-md-4">
            <p>Hello, {{ profile.name }}</p>
            <p>Here is your profile information:</p>
            {% for index, user in users %}
                {% include 'profile/partials/profile_table_item.twig' with {user: user} %}
            {% endfor %}
        </div>

        <div class="col-md-8">
            <div class="container">
                <h2>Your Trip Information</h2>
                <table class="table table-condensed table-hover">
                    <thead>
                    <tr>
                        <th>City</th>
                        <th>Country</th>
                        <th>Rating</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for index, trip in trips %}
                        {% include 'profile/partials/trip_table_item.twig' with {trip: trip} %}
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

{% endblock %}